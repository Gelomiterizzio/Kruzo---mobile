import { PlaceholderScreen } from '@/components/feedback/PlaceholderScreen'

export default function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Iniciar sesión"
      note="Email/contraseña + Google (plumbing listo) — UI en Fase 3."
    />
  )
}
