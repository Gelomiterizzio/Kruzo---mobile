import { PlaceholderScreen } from '@/components/feedback/PlaceholderScreen'

export default function ExploreScreen() {
  return (
    <PlaceholderScreen
      title="Explorar"
      note="Grid + filtros por categoría/zona (paridad web) — Fase 3."
    />
  )
}
