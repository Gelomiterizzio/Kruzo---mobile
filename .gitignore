# dependencies
node_modules/

# Expo
.expo/
.expo-export/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
android/
ios/

# Firebase native config (secret per project; provide locally / via EAS secret)
google-services.json
GoogleService-Info.plist

# Build artifacts (kept locally in builds/, not committed)
builds/
*.apk
*.aab

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# Env (secrets) — never commit
.env
.env.local
.env*.local

# Tests / coverage
coverage/

# IDE
*.log
